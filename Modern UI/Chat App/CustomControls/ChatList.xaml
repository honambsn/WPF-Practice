<UserControl x:Class="Chat_App.CustomControls.ChatList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Chat_App.CustomControls" xmlns:viewmodel="clr-namespace:Chat_App.ViewModels"
             mc:Ignorable="d" MinWidth="406">
    <UserControl.Resources>
        <DataTemplate x:Key="ChatButtonItemTemplate">
            <RadioButton GroupName="ChatButton">
                <Style TargetType="{x:Type ToggleButton}">
                    <Setter Property="SnapsToDevicePixels" Value="True"/>
                    <Setter Property="MinHeight" Value="80"/>
                    <Setter Property="Padding" Value="10"/>
                    <Setter Property="HorizontalAlignment" Value="Center"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Content" Value="{Binding ContactName}"/>

                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ToggleButton}">
                                <Border BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}" x:Name="border" CornerRadius="20" 
                                        Padding="{TemplateBinding Padding}"
                                        SnapsToDevicePixels="True">     
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!--#region Contact details-->
                                        <Grid Grid.Row="0">
                                            <Grid.ColumnDefinitions>
                                                <!--profile photo-->
                                                <ColumnDefinition Width="auto"/>

                                                <!--contact name & last message-->
                                                <ColumnDefinition/>

                                                <!--last message time-->
                                                <ColumnDefinition Width="auto"/>
                                            </Grid.ColumnDefinitions>

                                            <Ellipse>
                                                <Ellipse.Fill>
                                                    <ImageBrush ImageSource="{Binding ContactPhoto}" Stretch="UniformToFill"/>
                                                </Ellipse.Fill>
                                            </Ellipse>


                                            <!--contact name & last message-->
                                            <Grid Grid.Column="1" Margin="20,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <TextBlock Text="{Binding ContactName}" FontSize="20" FontWeight="SemiBold"/>

                                                <TextBlock Text="{Binding Message}" FontSize="20" FontWeight="SemiBold"/>
                                            </Grid>
                                        </Grid>
                                        <!--#endregion-->


                                        <!--#region Options-->

                                        <!--#endregion-->

                                    </Grid>

                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </RadioButton>
        </DataTemplate>
    </UserControl.Resources>
    
    <Grid Margin="25,15">
        <StackPanel>

            <!--title-->
            <StackPanel Orientation="Horizontal">
                <Path Data="{StaticResource allchats}" Fill="Black"/>
                <TextBlock Text="All Chats" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0"/>
            </StackPanel>
            
            <!--chatlist-->
            <ItemsControl ItemsSource="{Binding Chats}" ItemTemplate="{StaticResource ChatButtonItemTemplate}">
                <ItemsControl.DataContext>
                    <viewmodel:ViewModel/>
                </ItemsControl.DataContext>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>
